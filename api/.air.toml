root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/main"
  cmd = "go build -gcflags='all=-N -l' -o ./tmp/main ."
  include_ext = ["go"]
  exclude_dir = ["tmp"]
  full_bin = "dlv exec --accept-multiclient --continue --headless --listen=:2345 --api-version=2 ./tmp/main"
